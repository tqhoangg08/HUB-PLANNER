name: HUB notification crawler (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Report authoritative crawler
        run: echo 'Retired. School announcements are crawled by Cloudflare scheduled Workflows and written directly to D1.'
